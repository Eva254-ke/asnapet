---
import { waLinks } from '../data/site';
import { Image } from 'astro:assets';
import heroImage from '../assets/images/hero-packaging.webp';
---

<section class="hero" id="top" aria-labelledby="hero-title">
  <Image
    class="hero__image"
    src={heroImage}
    alt=""
    aria-hidden="true"
    loading="eager"
    fetchpriority="high"
    decoding="sync"
  />
  <div class="hero__media" aria-hidden="true"></div>
  <div class="hero__glow" aria-hidden="true"></div>

  <div class="container hero__container">
    <div class="hero__content">
      <h1 class="hero__title" id="hero-title">
        <span class="hero__title-main">Asnapet</span>
        <span class="hero__title-accent">Treasures</span>
      </h1>

      <p class="hero__tagline">Food and beverage packaging supplier in Nairobi</p>

      <p class="hero__subtitle">
        Takeaway containers, cups, bottles, pouches, paper bags and bakery cartons for food
        businesses. Order plain stock or branded packaging with your logo.
      </p>

      <div class="hero__actions">
        <a href={waLinks.catalog} class="btn btn-primary" target="_blank" rel="noopener noreferrer">
          Request catalog
          <span class="btn__arrow" aria-hidden="true">&rarr;</span>
        </a>
        <a href="#products" class="btn btn-outline">View packaging</a>
      </div>
    </div>
  </div>

  <a class="hero__scroll" href="#products" aria-label="Scroll to packaging lines">
    <span aria-hidden="true"></span>
  </a>
</section>
